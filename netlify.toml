[build]
  base    = "frontend"
  command = "npm run build"
  publish = ".netlify/static"

[build.environment]
  NODE_VERSION = "20"

[dev]
  command    = "npm --prefix frontend run dev"
  targetPort = 5173
  port       = 8888
  autoLaunch = false
